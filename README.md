# react-notes

# Before start 
npm install -g gulp

npm install -g bower

npm install -g protractor

# Instalation

npm install

webdriver-manager update --standalone

# Run development server
gulp

# Show builded app
gulp --build

# Run test
gulp test
